# Core dependencies for Zen MCP Server
-r requirements-base.txt

# Optional extras (install manually if you need those providers):
#   pip install -r requirements-openai.txt   # OpenAI SDK
#   pip install -r requirements-gemini.txt   # Google Gemini SDK

# Development dependencies live in requirements-dev.txt